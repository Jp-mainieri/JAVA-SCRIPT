var vel = 80;
var vel_perm = 70;

console.log(`A velocidade do seu carro é ${vel} km/h`);

if (vel > vel_perm) {

  console.log(`Voce ultrapassou a velocidade permitida (${vel_perm} km/h)`);

}
console.log(`Dirija sempre usando o cinto de segurança`);
